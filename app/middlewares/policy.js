const { checkMiddlewareFactory } = require("./check-middleware-factory");
const { POLICY_POLICY_NAME } = require("../constants/police-name");

module.exports = checkMiddlewareFactory(POLICY_POLICY_NAME);
